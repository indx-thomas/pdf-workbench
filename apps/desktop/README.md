# Desktop application

This directory reserves the desktop application boundary. The shell will be selected and scaffolded after the initial local-service and rendering spike.

The desktop application must remain a client of engine-neutral core operations. It must not call qpdf or PDFium directly.
